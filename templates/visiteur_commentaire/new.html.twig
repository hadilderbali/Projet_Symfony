
{% block title %}New Commentaire{% endblock %}

{% block body %}
    <h1>Create new Commentaire</h1>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

    {# Ajouter ici d'autres champs du formulaire #}
    {{ form_row(form.content) }}
    <button type="submit">Save</button>
    {{ form_end(form) }}


{% endblock %}
