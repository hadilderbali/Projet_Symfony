
{% block title %}Donation{% endblock %}

{% block body %}
    <h1>Donation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ donation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ donation.nom }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ donation.valeur }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ donation.cin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_visiteur_donation_index') }}">back to list</a>

    <a href="{{ path('app_visiteur_donation_edit', {'id': donation.id}) }}">edit</a>

    {{ include('visiteur_donation/_delete_form.html.twig') }}
{% endblock %}
